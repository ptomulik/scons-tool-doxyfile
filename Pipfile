[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[dev-packages]
scons-select = { path = "./deps/scons-select" }
twine = "*"
scons-tool-loader = ">=0.1.9"

[requires]
python = "2.7"
